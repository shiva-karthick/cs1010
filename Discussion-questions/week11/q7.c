#include "/home/shankar/Shiva/cs1010/include/common.h"
#include <stdio.h>
#include <stdlib.h>
int main(int argc, char const *argv[]) {
  /* I am not going to attempt this question, cos its time consuming. This
   * question tests on simple linear search of the patterns, which is pretty
   * simple if you can just iterate through each row. */
  return 0;
}
